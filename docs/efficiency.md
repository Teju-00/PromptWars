# Efficiency Strategy

The system uses serverless Google Cloud architecture.

Firestore stores structured civic datasets for fast retrieval.

Cloud Run executes eligibility validation logic on demand.

Gemini handles reasoning instead of local processing.

Google Maps API enables location lookup without maintaining location datasets locally.
